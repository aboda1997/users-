name: Unit Tests
on:
  [push ]

jobs:
  test:  
    runs-on: ubuntu-latest.
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: npm    
    
    - name: Run Tests
      run: npm test